/**
 * File overview:
 * Purpose: Source module for Postcss.Config.
 * Main exports: default export.
 * Major callers: Adjacent modules in the same feature area.
 * Side effects: none.
 * Read next: README.md
 */
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
