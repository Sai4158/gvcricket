# `components/shared`

Shared visual building blocks live here.

- Prefer adding broadly reusable UI primitives here instead of feature folders.
- Keep feature-specific business rules out of this folder.
- If a component needs match or director rules to work, it probably belongs in that feature folder instead.
