# `components/session`

Session listing and session setup UI live here.

- Use this folder for session cards, session list orchestration, and setup-specific modals.
- If logic becomes match-specific after setup completes, move it to `match` or `session-view`.
