# `match/page`

Umpire match-screen internals.

## Main entry

- `MatchPageScreen.jsx`: main umpire match implementation
- `MatchPageLayout.jsx`: render-only page shell and modal layout
- `match-page-helpers.js`: stage-card, score-sound, and local cache helpers
- Shared UI pieces still live one folder up beside the route-facing entry file

## Run this command

```bash
npm test
```
