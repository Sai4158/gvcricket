# `components/rules`

Rules-page sections and rule copy helpers live here.

- Keep this folder presentation-focused.
- If rule text needs structured data, prefer a dedicated helper file like `rule-sections.js`.
