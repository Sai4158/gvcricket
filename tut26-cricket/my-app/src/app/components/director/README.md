# `components/director`

Director console UI and adjacent helper modules live here.

- `DirectorConsoleClient.jsx` is the main screen shell.
- `DirectorConsoleChrome.jsx` holds reusable chrome pieces.
- `director-console-utils.js` holds selection, cache, and YouTube helpers.

Read this folder when changing director-only controls, music/effects tooling, or session auto-manage behavior.
