# `tests/security`

Security and hardening regression tests.

- Split by concern: validators/rate limits, access tokens/PINs, match safety, image safety, live commentary wording, and walkie server state.
- Shared imports and fixtures live in `security-test-helpers.js`.
